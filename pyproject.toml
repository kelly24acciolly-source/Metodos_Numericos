[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "metodos-numericos"
version = "0.1.0"
description = "Pacote de métodos numéricos
requires-python = ">=3.11"
dependencias = []
